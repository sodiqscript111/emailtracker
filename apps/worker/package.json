{
  "name": "@email-tracker/worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply email_tracker_db --local",
    "migrate:prod": "wrangler d1 migrations apply email_tracker_db --remote"
  },
  "dependencies": {
    "@email-tracker/shared": "*",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250214.0",
    "wrangler": "^4.113.0"
  }
}
