{
  "name": "@email-tracker/extension",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && esbuild src/content/gmail.ts --bundle --outfile=dist/content.js --target=chrome100 --format=iife && esbuild src/background/service-worker.ts --bundle --outfile=dist/background.js --target=chrome100 --format=iife",
    "watch": "vite build --watch"
  },
  "dependencies": {
    "@email-tracker/shared": "*"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.3",
    "vite": "^6.0.0"
  }
}
